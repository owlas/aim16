# AIM2016 Presentation

To build, you will need xelatex and ideally a full TeXLive installation.

To get the submodules `git submodule add && git submodule init`

![screenshot of title](screenshot.png
